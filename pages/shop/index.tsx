import CollectionOverview from '../../components/CollectionOverview'

const Shop: React.FC = () => {
    return (
        <div className="shop_page">
            <CollectionOverview />
        </div>
    );
};

export default Shop;
